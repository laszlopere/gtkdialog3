#!/bin/sh
# 
# We don't really need this first line but my text editor recognizes it and i
# like the syntax highlight.
#
function login_server() 
{
    echo "This is the login_server() function:"
    echo "  \$1: '$1'"
    echo "  \$2: '$2'"
    echo "  \$3: '$3'"
}
